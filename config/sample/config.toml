# Koch sample application configuration

app = "myApp"

# add desc
[owner]
name = "cozart shmoopler"
# add content

# add desc
[database]
# add content

# add desc
[server]
host = "localhost"
port = "80"
IP = "localhost"
chooseIP = false
secure = false
debugLog = true
userShutdown = false
certFile = "" # no cert 
keyFile = "" # no key
rootCA = "" # no root
# add content

# add desc
[modules]
# add content
