# Koch sample application configuration

app = "myApp"

# add desc
[owner]
name = "cozart shmoopler"
# add content

# add desc
[database]
# add content

# add desc
[logger]
level = "debug"

# add desc
[server]
host = "localhost"
port = "443"
IP = "localhost"
chooseIP = false
secure = false
debugLog = true
cmdEnable = false
certFile = "./sample/localhost.crt"
keyFile = "./sample/localhost.key"
rootCA = "./sample/rootCA.crt"
cacheMaxAge = 180
shutdownCode = -3
# add content

# add desc
[modules]
# add content
